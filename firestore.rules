rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full read/write access to all documents during development/prototyping
    match /{document=**} {
      allow read, write: if true;
    }
  }
}